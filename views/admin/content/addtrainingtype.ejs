
<script type="text/javascript">
    /********signUp form validation *********/
    $(document).ready(function() {
        $('#categoryAdd').submit(function(e) {
          
            var valid = $("#categoryAdd").valid();
            if (valid) {} else {
                e.preventDefault();
            }
        });
      $('#categoryAdd').validate({
            rules: {
                'name': 'required',
             },
            messages: {
                name: {
                    required: "Name can't be blank"
                }
           }

        });
});


/********end of form validation *********/
</script>
<div class="page-inner">

    <% if (message) { %>
        <div class="alert alert-danger">
            <%= message %>
        </div>
        <% } %>

           <div id="main-wrapper">
                <div class="row m-t-md">
                 <div class="col-sm-2"></div>
                 <div class="col-sm-8 col-sm-offset-3">
                    <h1 class="sign-user">Add Category</h1>

            

                    <!-- LOGIN FORM -->
                    <form action="/addtraining_type" method="POST" id="categoryAdd" enctype="multipart/form-data">
                        <div class="form-group">
                            <label>Training Title </label>
                            <input type="text" class="form-control" name="trainer_title" >
                        </div>
                        
                        
                        
                        
                        <div class="form-group">
                            <label>Training profile</label>
                           
                            <input type="file" class="form-control " id="e_Img_file" name=" training_profile">


                            
                        </div>
                        <button type="submit" class="btn btn-warning btn-lg">Save</button>

                    </form>
                </div>
            </div>
             </div>
            </div>
             </div>
            